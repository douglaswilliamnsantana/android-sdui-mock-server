{
  "name": "android-sdui-mock-server",
  "version": "1.0.0",
  "description": "Mock server for Android SDUI — serves screen JSON to the app",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix"
  },
  "dependencies": {
    "express": "^4.18.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "nodemon": "^3.0.1"
  }
}